#include "tlpi_hdr.h"

extern char **environ;

int main(int argc, char *argv[]) {
    // char **ep;
    // for (ep = environ; *ep != NULL; ep++) 
    //     puts(*ep);
    // exit(EXIT_SUCCESS);

    int a;
    scanf("%d", &a);
    int b = a + 10;
}